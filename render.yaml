services:
  - type: web
    name: terraria-wasm
    env: static
    staticPublishPath: .
    headers:
      - path: /*
        name: Cross-Origin-Embedder-Policy
        value: require-corp
      - path: /*
        name: Cross-Origin-Opener-Policy
        value: same-origin
